test = int(input());
    while(test):
        test-=1;
        s = input();
        ans = 0;
        n = len(s);
        for i in range(1,n):
            if(s[i-1]==s[i]):
                ans+=1;
        print(ans)
